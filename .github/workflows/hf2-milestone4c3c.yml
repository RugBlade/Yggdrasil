name: HF2 M4C3C - native resolution transport receipt

on:
  workflow_dispatch:
  push:
    branches:
      - stage8B/v0.8.2-conversation-completion-hf2
    paths:
      - 'patches/hf2/0010-hf2-native-resolution-transport-receipt.intent.patch'
      - 'tests/hf2_m4c3c/**'
      - '.github/workflows/hf2-milestone4c3c.yml'

permissions:
  contents: read

jobs:
  exact-baseline-candidate-replay:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      NOERON_DB_PATH: ./m4c3c-api-test-only.sqlite3
      NOERON_AUTONOMY_ENABLED: 'false'
      NOERON_TERRA_ENABLED: 'false'
      NOERON_LLM_BACKEND: mock
      NOERON_OWNER_TOKEN: ''
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Reconstruct byte-exact HF1 fixture
        shell: bash
        run: |
          set -euo pipefail
          FIX="$RUNNER_TEMP/ygg-hf1-exact"
          echo "FIX=$FIX" >> "$GITHUB_ENV"
          python scripts/verify_hf1_exact_fixture.py \
            --extract-dir "$FIX" \
            --extract-all \
            --evidence hf1-exact-identity-evidence.json

      - name: Add HF2 carrier-only helper modules
        shell: bash
        run: |
          set -euo pipefail
          for f in \
            conversation_premises.py \
            conversation_bridge.py \
            conversation_reasoning.py \
            conversation_proof.py \
            conversation_runtime_gate.py \
            conversation_actions.py \
            conversation_resolution.py
          do
            cp "src/noeron/$f" "$FIX/src/noeron/$f"
          done

      - name: Apply 0001 through 0009 sequentially
        shell: bash
        run: |
          set -euo pipefail
          cd "$FIX"
          git init -q
          git config user.name "Yggdrasil HF2 CI"
          git config user.email "hf2-ci@example.invalid"
          git add .
          git commit -qm "byte-exact HF1 fixture plus carrier helpers"
          for p in \
            0001-hf2-inference-core.patch \
            0002-hf2-current-turn-runtime-bridge.patch \
            0003-hf2-native-bounded-dialogue-reasoning.patch \
            0004-hf2-proof-provenance-ambiguity-runtime-gate.patch \
            0005-hf2-native-resolution-state-audit.patch \
            0006-hf2-speech-act-consequence-learning.patch \
            0007-hf2-owner-resolution-calibration.patch \
            0008-hf2-calibration-execution-boundary.patch \
            0009-hf2-native-resolution-content.patch
          do
            echo "CHECK $p"
            git apply --check "$GITHUB_WORKSPACE/patches/hf2/$p"
            git apply "$GITHUB_WORKSPACE/patches/hf2/$p"
            git diff --check
            echo "APPLIED $p"
          done
          git diff --stat | tee "$GITHUB_WORKSPACE/hf2-patch-chain-diffstat.txt"


      - name: Isolate exact post-0009 baseline
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p evidence
          cp hf1-exact-identity-evidence.json hf2-patch-chain-diffstat.txt evidence/
          cd "$FIX"
          git add .
          git commit -qm "verified exact post-0009 baseline"
          sha256sum src/noeron/orchestrator.py src/noeron/api.py

      - name: Install exact runtime dependencies
        run: python -m pip install --disable-pip-version-check -e "$FIX[dev]"

      - name: Verify published post0009 baseline before candidate
        env:
          PYTHONPATH: ${{ env.FIX }}/src
        shell: bash
        run: |
          set -euo pipefail
          python -m pytest -q tests/test_v082_hf2_*.py tests/hf2_m4c3a tests/hf2_m4c3b \
            --junitxml=evidence/m4c3c-baseline.xml | tee evidence/m4c3c-baseline.txt

      - name: Apply exact-anchor native-content intent
        shell: bash
        run: |
          set -euo pipefail
          python scripts/materialize_hf2_intent_patch.py             --root "$FIX"             --intent patches/hf2/0010-hf2-native-resolution-transport-receipt.intent.patch             --audit evidence/m4c3c-intent-audit.json
          git -C "$FIX" diff --check
          python -m compileall -q "$FIX/src/noeron"

      - name: Test complete HF2 suite plus M4C3C behaviors
        env:
          PYTHONPATH: ${{ env.FIX }}/src
        shell: bash
        run: |
          set -euo pipefail
          python -m pytest -q tests/test_v082_hf2_*.py tests/hf2_m4c3a tests/hf2_m4c3b tests/hf2_m4c3c             --junitxml=evidence/m4c3c-candidate-green.xml             | tee evidence/m4c3c-candidate-green.txt

      - name: Materialize 0010 only after candidate tests are green
        shell: bash
        run: |
          set -euo pipefail
          git -C "$FIX" diff --binary --full-index             > evidence/0010-hf2-native-resolution-transport-receipt.patch
          test -s evidence/0010-hf2-native-resolution-transport-receipt.patch
          git -C "$FIX" diff --stat | tee evidence/m4c3c-diffstat.txt

      - name: Prove 0010 on a fresh exact-HF1 extraction
        shell: bash
        run: |
          set -euo pipefail
          REPLAY="$RUNNER_TEMP/ygg-hf1-m4c3c-fresh-replay"
          echo "REPLAY=$REPLAY" >> "$GITHUB_ENV"
          python scripts/verify_hf1_exact_fixture.py --extract-dir "$REPLAY" --extract-all             --evidence evidence/m4c3c-replay-hf1-identity.json
          for f in conversation_premises.py conversation_bridge.py conversation_reasoning.py conversation_proof.py conversation_runtime_gate.py conversation_actions.py conversation_resolution.py; do
            cp "src/noeron/$f" "$REPLAY/src/noeron/$f"
          done
          cd "$REPLAY"
          git init -q
          for p in             0001-hf2-inference-core.patch             0002-hf2-current-turn-runtime-bridge.patch             0003-hf2-native-bounded-dialogue-reasoning.patch             0004-hf2-proof-provenance-ambiguity-runtime-gate.patch             0005-hf2-native-resolution-state-audit.patch             0006-hf2-speech-act-consequence-learning.patch             0007-hf2-owner-resolution-calibration.patch 0008-hf2-calibration-execution-boundary.patch 0009-hf2-native-resolution-content.patch
          do
            git apply --check "$GITHUB_WORKSPACE/patches/hf2/$p"
            git apply "$GITHUB_WORKSPACE/patches/hf2/$p"
            echo "APPLIED $p"
          done
          git apply --check "$GITHUB_WORKSPACE/evidence/0010-hf2-native-resolution-transport-receipt.patch"
          git apply "$GITHUB_WORKSPACE/evidence/0010-hf2-native-resolution-transport-receipt.patch"
          echo "APPLIED 0010-hf2-native-resolution-transport-receipt.patch"
          diff -qr --exclude=__pycache__ "$FIX/src/noeron" "$REPLAY/src/noeron"
          python -m compileall -q src/noeron

      - name: Test fresh replay runtime
        env:
          PYTHONPATH: ${{ env.REPLAY }}/src
          NOERON_DB_PATH: ${{ runner.temp }}/m4c3c-replay-api-test-only.sqlite3
        shell: bash
        run: |
          set -euo pipefail
          python -m pytest -q tests/test_v082_hf2_*.py tests/hf2_m4c3a tests/hf2_m4c3b tests/hf2_m4c3c             --junitxml=evidence/m4c3c-replay-green.xml             | tee evidence/m4c3c-replay-green.txt

      - name: Preserve source, carrier, hashes, and authority boundary
        shell: bash
        run: |
          set -euo pipefail
          tar --exclude=__pycache__ -czf evidence/m4c3c-exact-post0010-source.tar.gz -C "$REPLAY" src/noeron
          tar -czf evidence/m4c3c-carrier-evidence.tar.gz             patches/hf2 src/noeron/conversation_*.py tests/test_v082_hf2_*.py tests/hf2_m4c3a tests/hf2_m4c3b tests/hf2_m4c3c             scripts/verify_hf1_exact_fixture.py scripts/materialize_hf2_intent_patch.py             .github/workflows/hf2-milestone4c3c.yml
          (cd "$FIX" && git ls-files -z src/noeron | xargs -0 sha256sum)             > evidence/m4c3c-post0010-source-sha256.txt
          {
            echo "gate=M4C3C exact HF1 0001-0009 plus 0010 native-transport-receipt candidate and fresh replay"
            echo "source_commit=$GITHUB_SHA"
            echo "base_zip_sha256=79d04505988c6ed6a694e952d8fce4bd9265d1214a96baf490bb6280ecb08c29"
            echo "certification_claim=false"
            echo "live_runtime_touched=false"
            echo "canonical_db_touched=false"
            echo "native_resolution_actuator_connected=authenticated-owner-room-http-only"
            echo "calibration_executed=false"
            sha256sum evidence/0010-hf2-native-resolution-transport-receipt.patch               evidence/m4c3c-exact-post0010-source.tar.gz evidence/m4c3c-carrier-evidence.tar.gz
          } | tee evidence/m4c3c-boundary.txt

      - name: Upload actual results even on failure
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: hf2-m4c3c-native-resolution-transport-receipt
          path: evidence/
          if-no-files-found: error
